spring:
  datasource:
    url: jdbc:h2:mem:kanflow;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: postgres
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
