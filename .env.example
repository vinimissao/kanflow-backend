# Copia para ".env" na mesma pasta que docker-compose.yml (docker compose up).
# O Spring Boot em Maven local NÃO lê este ficheiro automaticamente — usa application.yml / variáveis no terminal.

PORT=9090
JWT_SECRET=dev-only-change-me-dev-only-change-me

POSTGRES_USER=kanflow
POSTGRES_PASSWORD=12345678
POSTGRES_DB=kanflow
